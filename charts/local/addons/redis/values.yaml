auth:
  enabled: true
  existingSecret: "redis"
  existingSecretPasswordKey: "REDIS_PASSWORD"

master:
  persistence:
    enabled: false

replica:
  persistence:
    enabled: false
  replicaCount: 1
sentinel:
  persistence:
    enabled: false